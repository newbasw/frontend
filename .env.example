# Where the storefront talks to the API.
# This origin must appear in the API's CORS_ORIGIN.
NEXT_PUBLIC_API_URL=https://backend-35ja.onrender.com

# This site's own address, used for canonical URLs and the sitemap.
NEXT_PUBLIC_SITE_URL=https://www.example.com

# Must match the API's value, so Vercel Cron can trigger the catalogue sync.
CRON_SECRET=
